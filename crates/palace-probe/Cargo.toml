[package]
name = "palace-probe"
description = "Headless Palace client probe: handshake, logon, room/user list, fixture capture."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "palace-probe"
path = "src/main.rs"

[dependencies]
palace-wire = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
